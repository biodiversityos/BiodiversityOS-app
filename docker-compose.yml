services:
  app:
    build:
      context: .
      args:
        NEXT_PUBLIC_INDEXER_URL: ${NEXT_PUBLIC_INDEXER_URL}
    ports:
      - "127.0.0.1:${PORT:-3001}:3000"
    env_file:
      - .env
    restart: unless-stopped
